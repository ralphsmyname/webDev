# Copy this file's values into your actual server environment
# (Apache SetEnv / php-fpm pool config / .env loader of your choice).
# config/database.php reads these via getenv().

DB_HOST=localhost
DB_PORT=3306
DB_NAME=hinlo_airsoft
DB_USER=root
DB_PASS=
